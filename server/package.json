{
  "name": "scxml-github-auth-server",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Express service that exchanges a GitHub OAuth code for an access token, keeping the GitHub OAuth client secret off the browser.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
